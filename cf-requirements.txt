# Cloudflare Workers Python 依赖
# 只包含纯Python库，无C扩展

# PDF处理 - 注意：pymupdf有C扩展，可能不兼容
# 如果pymupdf不可用，需要通过AI API或外部服务处理PDF
# pymupdf

# Word文档解析 - 使用纯Python实现（zipfile + xml）
# python-docx 已替换为纯Python实现

# HTTP请求 - Workers使用原生fetch，不需要requests
# requests

# Excel处理 - 已替换为CSV格式导出
# openpyxl

# PDF生成 - reportlab有C扩展，不兼容
# 如果需要PDF导出，建议：
# 1. 使用外部PDF生成服务
# 2. 使用纯Python的PDF库（功能有限）
# 3. 客户端生成PDF
# reportlab

# AI调用 - 使用原生fetch API，不需要openai库
# openai

# Web框架 - Workers不需要Flask
# Flask
# Werkzeug
# flask-cors

# ORM - Workers使用D1原生SQL，不需要SQLAlchemy
# sqlalchemy

# ========== 实际需要的依赖 ==========
# 注意：Cloudflare Workers Python运行时可能已经内置了部分标准库
# 以下依赖主要用于本地开发和测试

# 如果需要本地测试，可以安装：
# - 标准库（zipfile, xml.etree.ElementTree, csv, io, json等）已内置
# - 无需额外依赖

# 如果pymupdf在Workers中可用，可以添加：
# pymupdf==1.23.8

# 注意：实际部署时，Workers环境可能已经包含了必要的标准库
# 建议在Workers环境中测试后再决定是否需要添加依赖
