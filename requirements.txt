Flask==3.0.0
Werkzeug==3.0.1
PyPDF2==3.0.1
python-docx==1.1.0
requests==2.31.0
openpyxl==3.1.2
sqlalchemy>=2.0.44  # 需要2.0.44+以支持Python 3.13
flask-cors==4.0.0
openai==1.3.0

# OCR功能（可选，用于图片PDF识别）
# 推荐使用 PaddleOCR（中文识别准确率高，无需额外系统依赖）
# 安装: pip install paddlepaddle paddleocr
# 如果PaddleOCR安装失败，可以使用EasyOCR作为备选: pip install easyocr
# 如果都不需要，可以注释掉OCR相关代码

# PDF转图片（OCR需要）
# Windows: 需要安装 poppler，下载地址: https://github.com/oschwartz10612/poppler-windows/releases
# Linux: sudo apt-get install poppler-utils
# macOS: brew install poppler
# 或者使用 PyMuPDF 作为备选（无需额外依赖）: pip install PyMuPDF
# PyMuPDF==1.23.8  # 已安装，无需重新安装

